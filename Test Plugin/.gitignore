node_modules/
.DS_Store
.env
# build outputs (code.js carries the injected API key)
build/
/code.js  # built output at the plugin root (carries the key); src/ stays versioned
/ui.html  # built output at the plugin root (carries the key); src/ stays versioned
out/
