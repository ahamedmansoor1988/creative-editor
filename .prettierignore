node_modules
coverage
package-lock.json
# Standalone reference prototypes — preserved byte-for-byte, not app source.
dither-effects.html
gradient-patterns.html
# Existing app sources are excluded from the formatter in Stage 1 so that
# "preserve existing behavior" is verifiable by diff. Reformatting them is a
# deliberate, reviewable step scheduled for Stage 2.
public/app.js
public/style.css
public/index.html
server.js

# Vendored third-party, kept byte-for-byte (see THIRD-PARTY-NOTICES.md).
public/clipper2.mjs
# Engines ported verbatim from the author's standalone apps. SHADER-PROVENANCE.md
# records them as carried across unchanged, and reformatting would make that
# claim unverifiable by diff. Same Stage 1 reasoning as the app sources above.
public/glass.js
public/prism.js
public/capsule.js
public/blob.js
public/light.js
public/gradient.js
public/liquidgradient.js
public/prismflare.js

# Nested sub-project (a Figma plugin with its own build); formatted on its own.
Test Plugin/
# Standalone WebGL gradient lab, kept as reference material.
public/webgl-gradient.js
public/webgl-gradient.html
public/webgl-gradient.css

# A separate project that ended up inside this folder by accident (18 Sep
# 2026, moved in from the Desktop). Not app source; ignored so the gate stays
# meaningful. Remove this line once it is moved back out.
~stillspeaking

# Standalone labs the in-app effects were ported from (fxstack.js, shape-divider.js
# and light-beam-fx.js name them as provenance). Kept as reference pages, not app
# source, on the same terms as the WebGL gradient lab above.
public/light-beam.html
public/light-beam.js
public/light-beam.css
public/shape-composer.html
public/shape-composer.css
public/shape-divider-tool.js
# lab output
lab-out/
